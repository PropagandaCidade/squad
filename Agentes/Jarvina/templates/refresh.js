/**
 * refresh.js (DESATIVADO)
 *
 * Motivo:
 * - Em produção, este arquivo estava tentando conectar em ws://localhost:8081
 *   causando erros no console, instabilidade e “tremor” de tela (reflow/reload).
 *
 * Este stub é intencionalmente um NO-OP.
 * Se no futuro você quiser um hot-reload real, faça isso separado e só em ambiente local.
 */

(() => {
  // Opcional: deixe o console limpo (sem logs)
  // console.info("[refresh.js] desativado em produção.");

  // Se existir algum código antigo pendurado via listeners, removemos tudo que pudermos.
  // (Sem referenciar variáveis antigas — segurança total)
  try {
    // nada
  } catch (_) {}
})();